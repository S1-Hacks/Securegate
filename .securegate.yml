# SecureGate Policy Configuration
block_on_critical: true
block_on_high: true
block_on_medium: false

# Add CVE / rule IDs here to permanently suppress (never gate on them)
suppress: []

# PR label that bypasses the merge gate
override_label: "security-override"
